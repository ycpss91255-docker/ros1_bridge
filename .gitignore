.env
compose.yaml
coverage/
.Dockerfile.generated
# bridge.yaml is a per-clone symlink chosen by the operator
# (see README "Bridge config" section); not tracked.
bridge.yaml

# Claude Code session state when launched inside this sub-repo.
# Workspace-level Claude config lives in ycpss91255-docker/claude-workspace.
.claude/
